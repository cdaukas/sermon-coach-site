# Supabase (required for auth + dashboard)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only — webhooks and admin jobs (never expose to client)
SUPABASE_SERVICE_ROLE_KEY=

# Stripe (subscription webhooks + checkout)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Sermon evaluation (Step 6.3+)
ANTHROPIC_API_KEY=
EVALUATION_MODEL=claude-sonnet-4-6
# EVALUATION_USE_STUB=1

# YouTube caption fetch (dashboard transcript import — server-only)
SUPADATA_API_KEY=
