/** Auth-aware CTA landing — branches logged-in vs. new visitors. */
export const START_PATH = "/start";

/** Where logged-in visitors go after the optional acquisition question on /start. */
export const START_DESTINATION = "/dashboard/sermons/new";
